class Solution {
    public int fixedPoint(int[] A) {
        int c = A.length;
        int i=0;
        for (;i<c; i++) {
            if (i == A[i])
                return i;           
        }
        return -1;
    }
}
    

